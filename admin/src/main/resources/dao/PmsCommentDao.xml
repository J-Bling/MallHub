<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.admin.dao.pms.PmsCommentDao">

    <resultMap id="BaseResultMap" type="com.mall.mbg.model.PmsComment">
        <id column="id" jdbcType="BIGINT" property="id" />
        <result column="product_id" jdbcType="BIGINT" property="productId" />
        <result column="member_id" jdbcType="BIGINT" property="memberId" />
        <result column="member_nick_name" jdbcType="VARCHAR" property="memberNickName" />
        <result column="member_icon" jdbcType="VARCHAR" property="memberIcon" />
        <result column="product_name" jdbcType="VARCHAR" property="productName" />
        <result column="star" jdbcType="INTEGER" property="star" />
        <result column="content" jdbcType="VARCHAR" property="content" />
        <result column="pics" jdbcType="VARCHAR" property="pics" />
        <result column="replay_count" jdbcType="INTEGER" property="replayCount" />
        <result column="show_status" jdbcType="BIT" property="showStatus" />
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
    </resultMap>

    <select id="countByCondition" parameterType="com.mall.admin.domain.pms.ProductCommentQueryDTO"
            resultType="int">
        SELECT COUNT(*)
        FROM pms_comment
        <where>
            <if test="productId != null">AND product_id = #{productId}</if>
            <if test="memberId != null">AND member_id = #{memberId}</if>
            <if test="star != null">AND star = #{star}</if>
            <if test="showStatus != null">AND show_status = #{showStatus}</if>
            <if test="startTime != null">AND create_time &gt;= #{startTime}</if>
            <if test="endTime != null">AND create_time &lt;= #{endTime}</if>
        </where>
    </select>

    <!-- 分页查询满足条件的评论 -->
    <select id="selectByCondition" parameterType="com.mall.admin.domain.pms.ProductCommentQueryDTO"
            resultMap="BaseResultMap">
        SELECT *
        FROM pms_comment
        <where>
            <if test="productId != null">AND product_id = #{productId}</if>
            <if test="memberId != null">AND member_id = #{memberId}</if>
            <if test="star != null">AND star = #{star}</if>
            <if test="showStatus != null">AND show_status = #{showStatus}</if>
            <if test="startTime != null">AND create_time &gt;= #{startTime}</if>
            <if test="endTime != null">AND create_time &lt;= #{endTime}</if>
        </where>
        ORDER BY create_time DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <!-- 根据ID列表查询多个评论 -->
    <select id="selectByIds" parameterType="list" resultMap="BaseResultMap">
        SELECT *
        FROM pms_comment
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <!-- 批量更新评论的显示状态 -->
    <update id="updateShowStatus">
        UPDATE pms_comment
        SET show_status = #{showStatus}
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

</mapper>